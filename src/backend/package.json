{
    "name": "backend",
    "version": "1.0.0",
    "description": "Backend API for CyberPot Dashboard",
    "main": "index.js",
    "scripts": {
        "start": "ts-node src/server.ts",
        "dev": "ts-node --watch src/server.ts",
        "build": "tsc --build",
        "test": "echo \"No tests configured yet\" && exit 0",
        "lint": "echo \"No linter configured yet\" && exit 0"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dockerode": "^4.0.9",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "@types/cors": "^2.8.17",
        "@types/dockerode": "^4.0.0",
        "@types/express": "^4.17.21",
        "@types/node": "^20.10.0",
        "ts-node": "^10.9.1",
        "typescript": "^5.3.2"
    }
}